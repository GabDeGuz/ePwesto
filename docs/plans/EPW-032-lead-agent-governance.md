# EPW-032: Manual Lead Agent governance

Status: Completed pending human review.

Lead Agent coordinates this documentation task. Implementer owns orchestration
documents and `AGENTS.md`; QA/Evidence runs repository validation; Independent
Reviewer reviews the exact diff. Risk: low. No service, GitHub Projects,
worktree, automatic Git action, deployment, or Symphony is configured.

Validate links, `node scripts/verify/repository.mjs`, and `git diff --check`.
